section: screens
    DPC:
    RinLT:
        switchCorners = bottom-left
        switchCornerSize = 10
end

section: links
    RinLT:
        left(0,50) = DPC(0,60)
        left(50,100) = DPC(60,100)
    DPC:
        right(0,60) = RinLT(0,50)
        right(60,100) = RinLT(50,100)
end

section: options
    keystroke(Control+Shift+F1) = switchInDirection(left)
    keystroke(Control+Shift+F2) = switchInDirection(down)
    keystroke(Control+Shift+F3) = switchInDirection(up)
    keystroke(Control+Shift+F4) = switchInDirection(right)
end
