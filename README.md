# Rin's configuration files

A collection of my config files

# Notes

* grub folder's target is / instead of $HOME
* zsh requires zinit to be installed (zsh-init.sh)
* nvim requires vim-plug's autoload script (nvim-init.sh)
